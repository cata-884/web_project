Bugs critice (features broken)

  1. Fix reviews - mismatch URL frontend/backend
     - Frontend apeleaza GET /api/reviews?camping_id=X si POST /api/reviews -> 404
     - Backend expune GET /api/campings/{id}/reviews si POST /api/campings/{id}/reviews
     - Solutie: adauga aliasuri GET /api/reviews si POST /api/reviews in routes.php
       care citesc camping_id din query/body, SAU corecteaza URL-urile din camping-detail.js

  2. Fix reviews - camp gresit trimis de frontend
     - camping-detail.js trimite "comment" in body, dar ReviewsModel::create() citeste "content"
     - Schimba campul din camping-detail.js din comment -> content

  3. Fix bookings - guests_count vs guests
     - camping-detail.js:205 trimite guests_count: 2
     - BookingsController::store() citeste $body['guests'] ?? 1 -> mereu 1 oaspete
     - Schimba cheia din frontend in guests sau adauga fallback in backend

  4. Adauga buton "Adauga la Favorite" in pagina camping
     - Exista API (POST /api/sections/{id}/campings) si logica de remove in main.js
     - Lipseste UI-ul de add din camping-detail.js / camping.html
     - Flow: GET /api/sections -> gaseste sectiunea "Favorite" -> POST /api/sections/{favId}/campings


Dead code - de sters

  1. Inregistreaza sau sterge MediaController.php din routes.php
     - 4 metode implementate (upload/delete pentru campings si reviews), niciuna inregistrata in routes.php
     - Daca feature-ul e planificat: adauga rutele; daca nu: sterge fisierul

  2. Sterge render(), redirect() si $viewPath din Controller.php
     - Proiectul e REST pur, niciun controller nu apeleaza aceste metode
     - $viewPath e calculat inutil la fiecare request

  3. Decide soarta AdminController.php si OrganizersController.php
     - Nu exista pagini frontend pentru admin/organizer (dashboard.html a fost sters)
     - Daca nu se construieste panoul admin: sterge controlerele si rutele asociate din routes.php
     - Daca se planifica: marcheaza explicit ca WIP

  4. Sterge BookingsModel::findByCampingId()
     - Nicio ruta si niciun controller nu o apeleaza


Imbunatatiri (non-critice)

  1. Fix slug lookup in camping-detail.js
     - ?slug=X face GET /api/campings?limit=100 si cauta client-side -> se strica la >100 campinguri
     - Solutie A: campings.js sa linkeze cu ?id=X in loc de ?slug=X (harta deja face asta)
     - Solutie B: backend sa suporte GET /api/campings?slug=X

  2. GET /api/bookings?camping_id=X ignorat de backend
     - camping-detail.js:217 trimite camping_id ca query param, backend il ignora
     - Adauga filtru in BookingsController::index() sau documenteaza ca filtrul e client-side

  3. BookingsModel::calculatePrice() ignora parametrul guests
     - Docstring spune "price_per_night x nr_nopti x guests" dar implementarea face pricePerNight * nights
     - Fie corecteaza formula, fie schimba docstring-ul

  4. Elimina rutele de reviews neutilizate sau adauga UI
     - GET /api/reviews/{id}, PATCH /api/reviews/{id}, DELETE /api/reviews/{id} - fara frontend
     - GET /api/bookings/{id}, PATCH /api/bookings/{id}, POST /api/bookings/{id}/cancel - fara frontend
     - GET /api/campings/{id}/availability - fara frontend
     - POST/GET/PATCH/DELETE /api/sections (CRUD complet) - frontend foloseste doar index + campings subresource

   5. Schimbat formatul de calendar: american => european

   6. Filtrele din pagina de search functioneaza in ciuda lipsei de atribute din schema bazei de date